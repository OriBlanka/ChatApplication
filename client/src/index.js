import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.querySelector('#root')); //initialize root in the HTML -- react put all the context in the div="root" in index.html file in the root
